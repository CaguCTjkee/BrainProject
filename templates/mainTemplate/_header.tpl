<!DOCTYPE html>
<html lang="ru">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    {{\Core\System\Meta::getInstance()->getMetaHtml()}}

    <link rel="shortcut icon" href="{{\Core\System\Setup::$HOME}}/templates/{{\Core\System\Setup::$TEMPLATE}}/favicon.ico"/>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet"
          href="{{\Core\System\Setup::$HOME}}/templates/{{\Core\System\Setup::$TEMPLATE}}/resource/css/bootstrap.min.css"
          crossorigin="anonymous">
    <link rel="stylesheet"
          href="{{\Core\System\Setup::$HOME}}/templates/{{\Core\System\Setup::$TEMPLATE}}/resource/css/font-awesome.min.css"
          crossorigin="anonymous">
    <link rel="stylesheet"
          href="{{\Core\System\Setup::$HOME}}/templates/{{\Core\System\Setup::$TEMPLATE}}/resource/css/bootstrap-datetimepicker.min.css"
          crossorigin="anonymous">
    <link rel="stylesheet"
          href="{{\Core\System\Setup::$HOME}}/templates/{{\Core\System\Setup::$TEMPLATE}}/resource/css/style.css"
          crossorigin="anonymous">
</head>
<body>
